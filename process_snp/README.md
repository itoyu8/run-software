## count_haplotype_depth.sh

```bash
sbatch count_haplotype_depth.sh --vcf phased.vcf.gz --bam1 sample.h1.bam --bam2 sample.h2.bam --output path/to/output.tsv
```

Note: `--output` must specify full file path including filename.
